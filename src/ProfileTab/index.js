/**
 * @flow
 */

import React from 'react';
import {
    SafeAreaView,
    StatusBar,
    Text,
} from 'react-native';

const ProfileTab: () => React$Node = () => {
    return (
        <>
            <SafeAreaView>
                <Text>Profile</Text>
            </SafeAreaView>
        </>
    );
};

export default ProfileTab;
